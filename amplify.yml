version: 1
applications:
  - backend:
      phases:
        build:
          commands:
            - echo "********* IGNORE BACKEND CHECK/PUSH **********"
  - frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: /
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: client
